# Python dependencies pinned to work with tol-sdk 1.9.4
# These versions are for the ISOLATED Docker container running with SKIP_PREFECT=true
# NOTE: Prefect is NOT needed in the Docker container - only in the orchestration layer
# The container only needs tol-sdk and flow execution dependencies

# Core packages for flow execution
genomehubs>=2.10.14
tol-sdk==1.9.4

# Pinned dependencies for tol-sdk compatibility
# tol-sdk 1.9.4 requires these exact versions
click==8.1.7
pydantic==2.11.4

# Additional dependencies for data handling
boto3>=1.26.0
pyyaml>=6.0
requests>=2.28.0
python-dateutil>=2.8.2
